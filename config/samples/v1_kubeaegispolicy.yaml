apiVersion: cclab.kubeaegis.com/v1
kind: KubeAegisPolicy
metadata:
  labels:
    app.kubernetes.io/name: kubeaegis
    app.kubernetes.io/managed-by: kustomize
  name: kubeaegispolicy-sample
spec:
  # TODO(user): Add fields here
